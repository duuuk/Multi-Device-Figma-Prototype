{
  "name": "multifig-plugin",
  "version": "1.0.0",
  "description": "MultiFig Figma Plugin — Multi-device prototype sync engine",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.106.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
